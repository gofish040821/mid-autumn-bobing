# 月满中秋 · 在线博饼

> 六骰听月落，一桌争状元。

一个**真正的实时多人联机**中秋博饼网页小游戏。2~15 位朋友各自用自己的手机或电脑打开网页，进入同一桌，按座位轮流博饼，实时看到同一副骰子、同一个结果。

不是单机 Demo，不是一台电脑上轮流点，也不是前端 `Math.random()` 假装开奖——**骰子由服务器掷，所有人同时看到结果**。

---

## 目录

- [一分钟跑起来](#一分钟跑起来)
- [玩法](#玩法)
- [技术栈](#技术栈)
- [目录结构](#目录结构)
- [用手机一起玩（局域网）](#用手机一起玩局域网)
- [部署到公网（让别人在任何网络下都能进）](#部署到公网让别人在任何网络下都能进)
- [在一台电脑上开 4 个客户端测试](#在一台电脑上开-4-个客户端测试)
- [架构：为什么它是「真·联机」](#架构为什么它是真联机)
- [规则与判奖表](#规则与判奖表)
- [月华加持与追状元](#月华加持与追状元)
- [页脚：站点统计与创作者署名](#页脚站点统计与创作者署名)
- [想改点什么？](#想改点什么)
- [测试](#测试)
- [常见问题](#常见问题)

---

## 一分钟跑起来

需要 **Node.js ≥ 18**。

```bash
npm install     # 安装全部依赖（npm workspaces，一次装好三个包）
npm run dev     # 同时启动服务端(3001) 与 前端(5173)
```

然后打开 <http://localhost:5173>。

进入页面后点「开一张新桌」，会拿到一个 **5 位房间码**（如 `7K3FZ`）。想验证联机效果，点「复制邀请链接」发给**无痕窗口**或另一台设备，对方打开就自动坐进同一桌——不用手抄房间码。

> 人满 2 位，房主点「开始博饼」，或者凑满 15 人自动开局。
>
> 同一台服务器上可以同时开很多桌，互不干扰：朋友各自开各自的桌，链接发错了、房间码打错一个字母，都会明明白白告诉你「找不到这一桌」，而不是把你丢进一间空屋子干等。

### 其他命令

| 命令 | 作用 |
| --- | --- |
| `npm run dev` | 同时启动服务端与前端（开发用，支持热更新） |
| `npm run dev:server` | 只启动服务端（`tsx watch`，改代码自动重启） |
| `npm run dev:client` | 只启动前端（Vite） |
| `npm run typecheck` | 三个包全量类型检查 |
| `npm test` | 运行服务端全部单元测试（232 个，不需要启动服务） |
| `npm run test:e2e` | 端到端联机验收：起 4 个真实 Socket.IO 客户端连真实服务端，跑完整局（需先启动服务端） |
| `npm run test:multiroom` | 多房间隔离验收：开几张桌同时玩，逐条断言各桌的骰子、广播、玩家互不串线（需先启动服务端） |
| `npm run test:browser` | 浏览器验收：用系统 Chrome 真点一遍，截图存 `screenshots/`（需先 `npm run build && npm start`） |
| `npm run build` | 构建前端到 `client/dist` |
| `npm start` | 生产模式启动服务端（会自动托管 `client/dist`） |

生产部署最短路径：

```bash
npm run build && npm start
# 打开 http://<你的IP>:3001 —— 服务端会把前端和 Socket.IO 一起托管
```

---

## 玩法

1. **开桌 / 入席**——打开网页，点「开一张新桌」拿到房间码，或者用朋友给的房间码 / 邀请链接进他那一桌；取个雅号（或点「随机取名」），进入大厅。无需注册。
2. **开席**——满 2 人后房主点「开始博饼」；满 15 人则 5 秒后自动开席。
3. **轮流博饼**——按座位顺序，轮到你时点那颗朱砂大按钮。每回合 30 秒，超时由服务器自动代掷（不会卡住整桌人）；已经掉线的人只等 10 秒。
4. **看结果**——六颗骰子落定，按判奖表从高到低匹配奖项，发奖、加分、写进日志。
5. **追状元**——第一位博出状元骰型的人成为「状元候选」，其余每人按座位顺序再各追一次。**只有严格更高的状元等级才能反超，同等级先出现者优先。**
6. **结算**——追完一圈，最终状元拿走唯一的状元奖（基础分 + 100 分），出积分榜。

刷新页面、断网、切后台再回来，座位、昵称、积分、奖品、当前回合都会自动恢复。

---

## 技术栈

| 层 | 选型 |
| --- | --- |
| 前端 | React 18 + TypeScript + Vite + Zustand + Framer Motion + 原生 CSS |
| 实时通信 | Socket.IO（含自动重连、ACK 回执） |
| 后端 | Node.js + TypeScript + Express + Socket.IO |
| 校验 | Zod（所有客户端入参） |
| 测试 | Vitest |
| 仓库 | npm workspaces（`shared` / `server` / `client`） |
| 美术 | 纯 CSS + 内联 SVG，零外部图片、零版权音乐（音效用 Web Audio 现场合成） |

---

## 目录结构

```
mid-autumn-bobing/
├── package.json                    # workspaces 根，dev / build / test 入口
├── tsconfig.base.json
├── scripts/
│   ├── e2e-smoke.mjs               # 端到端联机验收（4 个真实客户端跑完整局）
│   ├── multiroom-smoke.mjs         # 多房间隔离验收（几张桌同时玩，断言互不串线）
│   └── browser-smoke.mjs           # 浏览器验收（真 Chrome 点一遍 + 截图）
│
├── shared/                         # 前后端共用的唯一真相
│   └── src/
│       ├── types.ts                # 全部数据契约（GameSnapshot / RollRecord / …）
│       ├── awards.ts               # ★ 判奖表：13 个奖项、积分、配色、演出时长
│       ├── timing.ts               # ★ 骰子动画 / 演出时长表（前后端共用同一张表）
│       ├── roomCode.ts             # 房间码的字母表、生成与校验
│       ├── events.ts               # Socket.IO 事件签名（类型安全）
│       ├── schemas.ts              # Zod 校验 + 随机雅号池
│       └── index.ts
│
├── server/
│   ├── vitest.config.ts
│   ├── src/
│   │   ├── index.ts                # Express + Socket.IO 启动，打印局域网地址
│   │   ├── config/
│   │   │   ├── gameConfig.ts       # ★ 全部可调参数（人数、超时、月华概率）
│   │   │   ├── prizes.ts           # ★ 奖品库存公式
│   │   │   └── scoring.ts          # 积分表
│   │   ├── game/
│   │   │   ├── RuleEngine.ts       # ★ 判奖（严格从高到低，命中即返回）
│   │   │   ├── DiceService.ts      # ★ 服务器唯一的掷骰入口 + 月华/保底
│   │   │   ├── PrizeService.ts     # 库存扣减与「已领完」
│   │   │   ├── ScoreService.ts     # 积分与排行榜
│   │   │   ├── ChampionService.ts  # 追状元队列、反超判定、月华值视图
│   │   │   ├── TurnService.ts      # turnId 生成与开奖请求校验
│   │   │   ├── GameStateMachine.ts # 阶段流转
│   │   │   ├── GameEngine.ts       # ★ 服务端权威状态机（唯一推进游戏的地方）
│   │   │   └── Clock.ts            # 可注入时钟（测试用 FakeClock）
│   │   ├── room/
│   │   │   ├── RoomManager.ts      # ★ 房间码 → GameEngine，按需建房 + 空房回收
│   │   │   └── PlayerManager.ts    # ★ socket → (房间码, playerId) 的路由依据
│   │   ├── stats/
│   │   │   └── SiteStats.ts         # 累计到访人数（按 guestId 去重，只在内存里）
│   │   └── socket/                 # Socket.IO 处理器与广播
│   └── tests/                      # 232 个测试
│       ├── rules.test.ts           # 穷举 6^6 = 46656 种组合
│       ├── dice.test.ts            # 骰子、月华概率、保底
│       ├── champion.test.ts        # 追状元队列与反超
│       ├── inventory.test.ts       # 奖品库存与发奖
│       ├── rooms.test.ts           # 多房间隔离、跨房 playerId 撞车、空房回收
│       └── engine.test.ts          # 端到端边界情况
│
└── client/
    └── src/
        ├── styles/theme.css        # ★ 设计系统（配色、字体、卷轴、印章、按钮）
        ├── audio/AudioManager.ts   # ★ Web Audio 现场合成音效（无音频文件）
        ├── stores/gameStore.ts     # Zustand 状态源
        ├── socket/socketClient.ts  # Socket.IO 客户端 + ACK 超时
        ├── lib/
        │   ├── roomLink.ts         # URL 上的 ?room= 房间码读写与邀请链接
        │   └── identity.ts         # 按房间隔离的座位凭证（localStorage）
        ├── hooks/useCountdown.ts   # 服务端时间校正的倒计时
        ├── components/
        │   ├── Scenery/            # 背景山水、满月、灯笼、桂花
        │   ├── Dice/               # 骰子与投骰动画
        │   ├── AwardCelebration/   # 13 种开奖演出
        │   ├── ChampionFlash/      # 金榜易主
        │   ├── RuleModal/          # 规则卷轴
        │   ├── PlayerList/ ScoreBoard/ PrizeInventory/
        │   ├── ChampionPanel/ MoonBlessing/ GameLog/ TurnTimer/
        │   └── Common/             # 印章、工具条、提示条、页脚（统计 + 署名）
        └── pages/                  # RulesPage / LobbyPage / GamePage / ResultPage
```

★ = 你大概率想改的文件。

---

## 用手机一起玩（局域网）

服务端和前端都监听了 `0.0.0.0`，同一 Wi-Fi 下的手机可以直接访问。

1. 在电脑上执行 `npm run dev`。
2. **启动时终端会打印局域网地址**，形如：

   ```
   ➜  本机:    http://localhost:5173
   ➜  局域网:  http://192.168.1.23:5173
   ```

   如果没有打印，也可以自己查：Windows 用 `ipconfig`，macOS/Linux 用 `ifconfig` 或 `ip addr`，找 `192.168.x.x` / `10.x.x.x` 这类地址。

3. 手机连**同一个 Wi-Fi**，浏览器打开 `http://192.168.1.23:5173`。
4. 每台设备取一个不同的雅号，就能坐在同一桌。

> **连不上？** 多半是电脑防火墙拦了 5173 / 3001 端口。
> - Windows：控制面板 → Windows Defender 防火墙 → 允许应用通过防火墙，勾选 Node.js；或临时关闭专用网络防火墙测试。
> - macOS：系统设置 → 网络 → 防火墙 → 选项，允许 Node 接受传入连接。
> - 另外确认路由器没有开启「AP 隔离 / 客户端隔离」，否则同一 Wi-Fi 下设备之间无法互访。

---

## 部署到公网（让别人在任何网络下都能进）

### 先说清楚：GitHub Pages / Vercel 这类托管**跑不了**这个游戏

这不是配置问题，是架构问题。这个游戏的三根支柱全在服务端：

- **骰子是服务端掷的**——客户端只能提交「我要掷」，不能提交点数；
- **房间、回合、30 秒超时**全在服务端内存里；
- 客户端与服务端靠 **Socket.IO 长连接**通信。

| 托管方式 | 能不能跑 |
| --- | --- |
| GitHub Pages / Netlify 静态托管 | ❌ 只发文件，不执行 Node，不维持长连接 |
| Vercel Functions | ❌ 2026 年 6 月虽然支持了 WebSocket，但**单条连接上限 5 分钟**，一局博饼远超这个时间 |
| **Render / Railway / Fly.io / 自己的服务器** | ✅ 能跑常驻 Node 进程与长连接 |

把前端单独丢到静态托管上，页面能打开、能画，但一点「开始博饼」就永远卡在连接中——**看起来像坏了，其实是根本没有服务端**。

### 部署只要一个服务

`server/src/index.ts` 里 Express 既托管 `client/dist` 静态页、又挂 Socket.IO，**前后端本来就在同一个进程、同一个源上**。因此：

- 不需要「前端一个域名 + 后端一个域名」；
- 不存在跨域问题，不用配 CORS；
- 平台给你一个地址就够用。

### Render（仓库里已带好 `render.yaml`）

1. 把仓库推到 GitHub。
2. 打开 [render.com](https://render.com)，用 GitHub 账号登录。
3. **New +** → **Blueprint** → 选中本仓库。Render 会读 `render.yaml`，自动填好构建与启动命令。
4. 等首次构建完成（约 2~5 分钟），拿到形如 `https://mid-autumn-bobing-xxxx.onrender.com` 的地址。

这个地址可以发给**任何网络下的人**（微信直接发就行），不需要同一个 Wi-Fi。

想手动填的话就两条：

| 项 | 值 |
| --- | --- |
| Build Command | `npm install --include=dev && npm run build` |
| Start Command | `npm start` |

### 免费档的真实限制（是取舍，不是 bug）

1. **15 分钟没有入站流量就休眠**，下次访问冷启动约 1 分钟。玩的时候每 30 秒有人掷骰，有流量就不会休眠。
2. **所有房间的状态都是纯内存的**（`RoomManager` 持有一张 `Map<房间码, GameEngine>`）。进程一休眠或重新部署，**所有桌**的牌局一起没——免费档是单进程，做不到「只掉一桌」。对 5~10 分钟一局的牌来说这个代价可以接受。
3. 玩家**短暂掉线**是安全的：`guestId + sessionToken` 会重连回原座位，`ABANDON_GRACE_MS` 兜住「全员掉线导致房间死锁」。但**服务进程重启**救不回来。

想彻底避免休眠，就升到付费档，或者干脆用自己电脑 + 局域网 / 内网穿透。

### 部署前必查的一件事

`client/src/socket/socketClient.ts` 里的连接地址**必须由同源推导**。这里曾经写死过「非 localhost 就直连 `:3001`」，在云平台上必然连不上——平台只给一个 https 域名，没有 3001 这个端口。**这个 bug 在本地怎么测都测不出来**，因为本地永远命中 localhost 那一支。

要复现云端的失败条件，可以在本地用「非 localhost 域名 + 非 3001 端口」模拟：

```bash
PORT=10000 npm start
# 另开一个终端（127.0.0.2 属于 127.0.0.0/8 回环段，但不是 localhost）
BROWSER_TEST_URL=http://127.0.0.2:10000 npm run test:browser
```

---

## 在一台电脑上开 4 个客户端测试

一个人也能验证联机逻辑：

**方法一：多个窗口（最简单）**

1. 普通窗口打开 <http://localhost:5173>，点「开一张新桌」，拿到房间码；
2. 点「复制邀请链接」——链接形如 `http://localhost:5173/?room=7K3FZ`；
3. 把这个链接分别粘进 3 个无痕窗口。

**关键是第 2 步**：直接在新窗口里敲 `localhost:5173` 是**没有房间码**的，四个窗口会各自开各自的桌，然后你会以为联机坏了。房间码就带在那个 `?room=` 上，链接复制过去才坐得进同一桌。

> 必须用无痕窗口：身份靠 `localStorage` 里的 `guestId` 区分，同一个浏览器配置文件会被认成同一个人（服务端会拒绝重复入席并提示「这个浏览器已经有座位了」）。

**方法二：不同浏览器**

Chrome / Edge / Firefox / Safari 各开一个，都把那条 `?room=` 邀请链接粘进去。

**方法三：手机 + 电脑混合**

电脑开 2 个无痕窗口，2 台手机各开一个（手机扫不出码就手输那 5 位房间码）——最接近真实聚会场景。

**验证清单：**

- [ ] 4 个窗口都能进大厅，看到彼此
- [ ] 房主点「开始博饼」后，4 个窗口**同时**跳到游戏页
- [ ] 轮到某人时，只有**他的**「博饼」按钮是亮的
- [ ] 他点下去，**所有**窗口同时播放同一段骰子动画，落定后是同一组点数
- [ ] 有人博出四颗四点 → 所有窗口同时进入追状元
- [ ] 关掉一个窗口再打开 → 自动回到原座位，积分奖品都还在
- [ ] 某个窗口什么都不做 → 30 秒后所有人看到「系统替他掷出了骰子」

---

## 架构：为什么它是「真·联机」

### 服务端权威（这是整个项目的地基）

**客户端只能做一件事：说「我要博饼」。**

```ts
// shared/src/events.ts —— 客户端能发的全部内容
game:roll(turnId: string, actionId: string)   // 没有 dice、没有 award、没有 score
```

- 骰子由 `server/src/game/DiceService.ts` 生成；它甚至不接受外部的随机源，只接受可注入的 `Rng`。
- 奖项由 `RuleEngine` 判定，积分、库存、状元全部在服务端计算。
- 客户端提交的 `dice` / `award` / `score` / `inventory` / `champion` 一律**不存在于协议里**，想伪造也无从下手。

### 事件 + 全量快照广播

每次状态变化，服务端同时下发：

1. **语义事件**（`roll:started`、`roll:result`、`champion:updated`…）——告诉客户端「放什么动画、放什么音效」；
2. **完整快照** `GameSnapshot`——携带 `stateVersion`。

客户端用 `stateVersion` 做单调校验，**永远不用旧状态覆盖新状态**。因此丢包、乱序、切后台回来都能自愈：快照一到，界面自动对齐。

### 服务端时间权威

倒计时以服务端下发的 `deadlineAt` 为准，客户端用 `serverTimeOffset`（= `snapshot.serverTime - Date.now()`）校正本地时钟。**手机时间不准也不会显示错乱**，而且客户端倒计时纯粹是展示——真正的超时判定在服务端。

### 防重复开奖

每个回合有唯一 `turnId`，每次点击带唯一 `actionId`。服务端用 `(turnId, actionId)` 做幂等键，并且回合状态机严格限制 `WAITING → ROLLING → RESOLVING`。

所以：**双击按钮只会博一次**，弱网重发也只会博一次。

### 掉线与重连

- 身份 = `guestId`（浏览器生成）+ `sessionToken`（服务端下发）。
- 刷新 / 断线重连时带上 token，服务端把连接重新绑回原座位——**昵称、座位、积分、奖品、游戏阶段全部恢复**。
- 游戏进行中，掉线玩家**永远不被移出座位**，只标记 `offline`；轮到他时照样自动代掷，整桌不会卡住。
- **代掷的等待时间分两档**：在线的人是 30 秒（给他思考的时间），**已经离线的人只有 10 秒**。人都不在了，等满 30 秒只是让一桌人陪着一个空座位干等；掉线那一刻如果正好轮到他，截止时间也会立刻从 30 秒缩短到 10 秒（`deadlineAt` 一起改，客户端的倒计时才不会和服务端对不上）。
- 只有在**等待大厅**里，掉线超过 60 秒才腾出座位给别人。
- **房主走了，这一桌就作废**（详见下一节）。
- **散场回收**：牌局进行中如果**一个人都不在线**了（大家都关了页面、断网、手机没电），房间不会永远卡死。等 2 分钟仍然没人回来，这一局就作废、房间退回大厅，下一组人推门进来就能直接开新局。

### 多房间：一桌一个引擎

服务器同一进程里可以并行开很多桌。结构很薄：

```
RoomManager            Map<房间码, GameEngine>     ← 只管「哪张桌在哪」
  └─ GameEngine        玩家 / 骰子 / 库存 / 计时器   ← 完全自包含，不知道别的桌存在
PlayerManager          socket.id → (房间码, playerId)  ← 路由依据
```

桌与桌之间**没有任何共享状态**，所以「A 桌在追状元」和「B 桌刚开席」天然互不影响。要不要加锁、要不要隔离状态，这类问题在结构上就不存在。

几个刻意的选择：

- **房间按需创建，不预建房间池。** 第一次有人点「开一张新桌」才建，空置满 2 分钟回收（记录时间戳 + 定期扫描，而不是每桌挂一个定时器——后者会在反复创建回收中攒下一堆定时器句柄）。
- **房间码由服务端生成，客户端不能指定。** 字母表剔掉了 `0/O/1/I/L`，31 个字符取 5 位 ≈ 2860 万种组合；建房时仍对着现存房间查重。
- **进不存在的房间不会顺手建一张。** 打错一个字母就凭空多出一间空房，而人会坐在里面等一个永远不会来的朋友。所以 `room:join` 只查不改，建房必须走 `room:create`。这条有测试盯着。
- **路由依据是 socket 自己记住的房间**（入席成功时写进 `socket.data`），不是 payload。客户端只能在过闸那一刻选一次房间，之后所有动作都发生在同一张桌，没法中途「跳到」别人的房间里去。换桌时会先退出旧的 Socket.IO 房间，否则旧桌的广播还会继续漏过来。
- **`playerId` 只在房间内唯一。** 每张桌都从 `player_0001` 开始数，所以 A 桌和 B 桌一定各有一个 `player_0001`，而他们**不是同一个人**。socket → 玩家的绑定必须带上房间码，否则 B 桌的人会拿到 A 桌的 playerId，接着用别人的身份掷骰子——两边看起来都「正常」，只有积分记错了人。这是整个改造里最隐蔽的一处，`rooms.test.ts` 和 `npm run test:multiroom` 都在盯它。
- **每次入席，当事人会额外收到一份快照。** `engine.join()` 广播 `room:playerJoined` 的那一刻，这个 socket 还没进房间，收不到自己的入场广播；不补一份，新人就得干等下一次广播才同步。顺序上仍然先 `engine.join` 再 `socket.join`——这样入席失败时（满员、身份冲突）完全不碰 socket 的房间归属，换桌失败不至于把人从原来的桌上踢下来。
- **换桌会回头告诉旧桌「这个人走了」。** 少了这句，旧桌会永久留着一个「在线」的幽灵座位：人明明在别桌玩，这边还占着位子、人数也算他一份，而且因为 socket 已经退出旧房间，它的 `disconnect` 永远不会来，谁也清不掉。大厅里直接退座，牌局进行中则只标记离线——中途抽走座位会把那一局搅乱。

### 房主离席 = 整桌作废

**开桌的人一走，这一桌立刻解散**，剩下的人收到一句「房主已离席，这一桌散了」并被请回入席页。房间从列表里消失，所有人的座位凭证一并作废。

刻意**不做房主移交**：这张桌是房主开的、房间码是他发出去的、牌局也是他起的头。换个人接手，剩下的人是在玩一局「主人已经走了」的牌，谁都不知道还该不该继续——不如干脆散伙，让剩下的人重新开一张桌来得清楚。

#### 代价是明摆着的

**房主只要断线，全桌陪葬。** 具体包括：

- 刷新页面
- 切到别的标签页 / 切到别的 App
- **手机息屏**

服务端认定「人走了」只需要一次传输层断开，以上三种都会触发。所以：

> ⚠️ **手机上开桌 → 复制邀请链接 → 切到微信粘贴发送 → 手机息屏，这一桌就没了。**
> 朋友们点开链接会看到「找不到这一桌」。

这条与空房回收里那句「连续两次扫描才回收，中间那段时间留给『创建 → 朋友点进来』」的心意是**冲突**的：回收逻辑特意留了宽限期，而房主离席是零宽限。这不是疏漏，是按「一断线立刻作废」的要求做的选择，但用手机发链接时确实会撞上。

真要绕开，有两个不改代码的办法：**先把人都拉进来再开桌**，或者**桌面端开桌发链接**（电脑不会息屏）。如果希望房主短暂掉线还能回来续上，那就得给「房主离席」加一个宽限期——目前是**没有**的。

#### 实现上容易踩的两个坑

- **判断必须在引擎动手之前。** `engine.disconnect()` 一调用，`ensureHost()` 会立刻把房主移交给别人；再想问「刚走的这位是不是房主」，答案永远是「不是」。所以是 socket 层先看 `engine.hostId`，确认是房主就直接把整桌关掉。
- **顺序不能反。** 先 `io.to(roomId).emit('room:closed')`，再 `players.unbindRoom()`，最后才 `rooms.destroy()`。等 socket 都退出了房间，这条通知就没人收得到了。

客户端这边还有一处**不能想当然**的地方：收到 `room:closed` 时**不要把 `snapshot` 清成 `null`**。退场是靠 `hasJoined` 触发的，而 `AnimatePresence` 的 `mode="wait"` 会把旧页面继续挂着、等它淡出完（340ms）才挂新页面——这段窗口里如果快照是空的，游戏页整棵子树当场渲染成空，退场动画的「已完成」回调再也不会触发，新页面永远等不到上场机会，**用户看到的是白屏**。这个 bug 就是 `npm run test:browser` 抓出来的，`resync` 遇到 `INVALID_SESSION` 时也是同样的处理（只置 `hasJoined`，不动快照）。

### 前后端节奏一致

骰子动画播多久、演出放多长、延迟多久进下一回合——这三件事共用 `shared/src/timing.ts` 里的一张表：

```ts
export const ROLL_TIMING: Record<AwardId, { diceMs: number; celebrationMs: number }> = { … }
```

服务端读它来决定「什么时候推进下一回合」，客户端读它来决定「动画播多久」。**两边不可能跑偏**；想整体调快调慢，只改这一个文件。

---

## 规则与判奖表

严格**从高到低**匹配，命中即返回（`shared/src/awards.ts` 的数组顺序就是优先级）：

| 优先级 | 奖项 | 骰型 | 积分 | 类型 |
| ---: | --- | --- | ---: | --- |
| 1 | 状元插金花 | 四颗四点 + 两颗一点 | 120 | 状元 |
| 2 | 六杯红 | 六颗四点 | 100 | 状元 |
| 3 | 遍地锦 | 六颗一点 | 80 | 状元 |
| 4 | 六抔黑 | 六颗相同（2/3/5/6） | 60 | 状元 |
| 5 | 五红 | 五颗四点 | 50 | 状元 |
| 6 | 五子登科 | 五颗相同（非 4） | 40 | 状元 |
| 7 | 四点红 | 四颗四点 | 30 | 状元候选 |
| 8 | 对堂 | 1~6 各一颗 | 15 | 普通 |
| 9 | 四进 | 四颗相同（非 4） | 8 | 普通 |
| 10 | 三红 | 三颗四点 | 5 | 普通 |
| 11 | 二举 | 两颗四点 | 2 | 普通 |
| 12 | 一秀 | 一颗四点 | 1 | 普通 |
| 13 | 无奖 | 其余 | 0 | — |

**七个状元等级共用一个「状元」奖品**，全桌只有一个，只发给最终状元。七个等级的强弱由 `championRank` 决定（插金花 7 → 四点红 1），追状元时用它比较。

### 奖品库存

| 奖品 | 数量公式 | 2 人局 | 15 人局 |
| --- | --- | ---: | ---: |
| 一秀 | 4 × 人数 | 8 | 60 |
| 二举 | 2 × 人数 | 4 | 30 |
| 三红 | 人数 | 2 | 15 |
| 四进 | ⌈人数 / 2⌉ | 1 | 8 |
| 对堂 | max(1, ⌈人数 / 5⌉) | 1 | 3 |
| 状元 | 1 | 1 | 1 |

**库存领完不补**：骰型照常显示，但标注「奖品已领完」，不发奖也不加分。

---

## 月华加持与追状元

### 为什么要设计这个

纯随机的博饼有个现实问题：**一秀概率约 37%，而状元档只有约 1.2%**（穷举 46656 种骰型，状元档（七个等级合计）共 561 种，正好 1.2024%）。这个数字由传统判奖表定死，**改不了**——它是博饼之所以是博饼的地方。

于是加了两层设计，一个负责「别让人干等」，一个负责「让状元仍然是稀罕事」：

- **月华加持**：给一个**明明白白写在规则里**的加速机制，但**概率压得很低**；
- **状元保底**：兜住下限，`5N` 次之内必定出现首位状元。

### 具体规则

- **第一轮（前 N 次，N = 人数）完全按正常六面骰随机**，不做任何干预。这一轮出的状元是纯天命，跟机制无关。
- **第二轮起**：每次掷骰前额外判定一次「是否催生状元」，首次 **0.5%**，之后每掷一次未出状元 **+0.2%**，**封顶 3%**。
  - 也就是说，加持在最强的时候（3%）也只是自然概率（1.2%）的 2.5 倍，开局时（0.5%）甚至**只有自然概率的四成**。
  - 调低这一组数是刻意的：状元要重新变成一件值得站起来喊的事，而不是被机制催出来的常规产物。
- **最多五个完整轮次（第 5N 次）必定出现首位状元**。
- **追状元阶段机制关闭**，纯随机。

### 调低之后，状元到底还稀不稀罕

用 20000 局模拟跑出来的分布（状元**第一次**出现的时机）：

| 人数 | 状元的来源：保底 / 加持 / 首轮天命 | 平均第几掷出现 | 共几掷保底 |
| --- | --- | --- | --- |
| 2 | 83% / 7% / 2% | 9.3 | 10 |
| 4 | 60% / 21% / 5% | 16.6 | 20 |
| 6 | 41% / 32% / 7% | 22.0 | 30 |
| 10 | 20% / 45% / 12% | 29.5 | 50 |
| 15 | 8% / 50% / 17% | 35.0 | 75 |

调参前（3% 起、封顶 15%）同一张表的 4 人局是「16% / 69% / 5%、平均第 12.7 掷」——**状元主要是被加持催出来的**。现在 4 人局里六成的状元落在最后的保底时刻，加持只是偶尔顺手推一把。

> ⚠️ **注意一件事：每局牌都一定有状元。** 保底决定了这一点，而状元是本游戏的头奖、追状元环节也靠它启动。所以「把状元概率调低」能调的是**什么时候出现、出现几次易主**，而不是**这局有没有状元**。
>
> 想让某些牌局干脆没有状元，只能把 `CHAMPION_GUARANTEE_ROUND` 拉大（甚至取消保底），代价是牌局拖长、还可能在一局毫无高潮的情况下草草结束。当前值 5 是折中，不建议动。

### 界面上怎么呈现

界面上有一个「月华值」——一枚从缺到圆的月亮加一条进度条，配文「月华未起 → 月华渐满 → 月华将满 · 状元将至 → 状元已出 · 月华归隐」。

**界面永远不会说「下一把必出状元」**，只提示氛围；具体概率数字也不展示给玩家，规则弹窗同样不写百分比。这条约束有测试守着（`champion.test.ts` 里断言文案不含「必出 / 必中 / 保证 / 一定 / 必定」）。

### 追状元

首位状元出现后：

1. 他从**下家开始**按座位顺序，其余每人各追加**一次**。
2. 追的人博出**严格更高**的状元等级才能反超；**同等级先出现者优先**。
3. 只追**一圈**（N−1 次），不延长。
4. 追完后，最终状元拿走唯一的状元奖：**他的状元骰型基础分 + 100 分**。

这里有个容易踩的坑：**追状元阶段掷出的状元骰型在「掷出那一刻」不发奖也不加分**（库里那份状元的归属还没定），要等结算才统一发给最终状元。被反超的人一分也拿不到。这条也有测试守着。

---

## 页脚：站点统计与创作者署名

大厅、规则页、结算页的底部都有一条页脚，两样东西：

```
创作者 @gofish040821 ↗
37 人到访 · 4 人在席 · 4 桌开着
自本次开服以来
```

### 三个数字分别是什么

| 数字 | 含义 | 怎么算的 |
| --- | --- | --- |
| **人到访** | 累计到访人数 | 浏览器里那个匿名 `guestId` 的**去重计数** |
| **人在席** | 此刻在线人数 | 已经入席、且连接还在的人（不是裸连接数） |
| **桌开着** | 此刻开着的桌数 | `RoomManager` 里现存的房间数 |

「人到访」按 `guestId` 去重，所以**同一个人换桌、刷新页面、开两个标签页、断线重连，都只算一次**——计的是人头，不是 PV。

它由服务端在三个时机广播（`stats:updated`）：新连接建立、有人**第一次**到访、有人断开。最后那条特别处理过：只有真·新面孔才值得惊动全站广播一次，同一个人的重连不该让所有人的界面刷屏。

> `guestId` 是客户端自己生成、自己上报的，没有登录态可校验，所以**这个数字挡不住刻意刷**（改一个 id 就是一张新面孔）。它是个气氛数字，不是需要防篡改的指标——为一个页脚计数器上一套身份校验，投入和收益完全不成比例。真要在意，就在服务端按 IP 限流，那又是另一件事了。

### ⚠️ 这个数字会归零（请照实理解）

统计和房间一样**全在内存里**（`server/src/stats/SiteStats.ts`），进程重启就归零。这是刻意的取舍：本项目的原则是不引数据库——一局游戏才几分钟，为此上一个持久化存储不值得。

代价是：**它只是「自本次开服以来」，不是历史总量。** 所以界面文案写的就是「自本次开服以来」，README 也这么写。在 Render 免费档上，服务闲置 15 分钟会休眠、每次重新部署也会重启进程，这个数字会跟着重来。

> 想要真正跨重启的累计，得引入外置存储（一个 JSON 文件在免费档上也活不过一次重新部署）。那是另一个取舍，代码里没有假装它已经解决了。

### 创作者署名

页脚那行 `@gofish040821` 指向 <https://github.com/gofish040821>，**只有文字和一个内联 SVG 小图标，没有任何外部图片**。

这一点是硬约束，不是审美偏好：

- 项目规则里写着「不要 hotlink 来源不明的网络图片」——拉一个 GitHub 头像就是在 hotlink；
- `npm run test:browser` 里有好几条「没有访问任何外部网络」的断言，外链图片会让它们直接挂掉。

浏览器验收也守着署名本身（指向哪、显示成什么、页脚里有没有 `<img>`），见 `scripts/browser-smoke.mjs` 的 `[2b]` 小节。

---

## 想改点什么？

### 改判奖规则 / 积分

编辑 **`shared/src/awards.ts`**。

- 改积分：改对应奖项的 `score`。
- 改判定条件：改 **`server/src/game/RuleEngine.ts`** 里的 `PREDICATES`。
- **加一个新奖项**：在 `shared/src/types.ts` 的 `AwardId` 里加 id → 在 `awards.ts` 里**插入到正确的顺序位置**（`order` 必须连续，数组顺序就是优先级）→ 在 `PREDICATES` 里加判定。

> ⚠️ 顺序错了就会出现「低等级覆盖高等级」的经典 bug。`rules.test.ts` 会穷举 46656 种组合来抓这种错误。

### 改奖品数量

编辑 **`server/src/config/prizes.ts`** 的 `buildInventory()`。

改完建议同步更新 `shared/src/awards.ts` 里各奖项的 `rule` 文案（规则弹窗会显示）。

### 改月华加持概率 / 保底

编辑 **`server/src/config/gameConfig.ts`**：

```ts
export const MOON_BLESSING_START_ROUND    = 2;     // 从第几轮开始加持
export const MOON_BLESSING_INITIAL_RATE   = 0.005; // 首次额外概率（0.5%）
export const MOON_BLESSING_RATE_INCREMENT = 0.002; // 每次递增（0.2%）
export const MOON_BLESSING_MAX_RATE       = 0.03;  // 上限（3%）
export const CHAMPION_GUARANTEE_ROUND     = 5;     // 最多几轮内必出首位状元（5N 次）
export const ABANDON_GRACE_MS             = 120_000; // 全员离线多久算散场
export const CHAMPION_WEIGHTS = { … };             // 加持时各状元等级的权重
```

> 想把状元调得更罕见，只需把这四个数一起往下压（保持初始 < 上限即可）。
> 但**别指望能压到「这局没有状元」**——保底那一层还在，见上一节的说明。
> `dice.test.ts` 里有一条断言守着「上限必须真的能在保底之前被摸到」，
> 所以把上限压得比 `初始值 + 增量 × 机会次数` 还大是没意义的（那个上限永远不会生效）。

同一个文件里还有回合超时、人数上下限、自动开局倒计时等。**玩法参数基本都集中在这一个文件**。

### 改人数上下限

`server/src/config/gameConfig.ts` 的 `MIN_PLAYERS` / `MAX_PLAYERS`。

### 改演出节奏（骰子动画多快、庆祝多久）

编辑 **`shared/src/timing.ts`** 的 `ROLL_TIMING`。前后端同时生效，不会脱节。

想整体加速（比如做演示），把 `diceMs` 和 `celebrationMs` 统一乘个系数即可。

### 改视觉：颜色、字体、留白

编辑 **`client/src/styles/theme.css`**。全部设计 token 都在顶部 `:root` 里：

```css
--paper: #f3e9d2;    /* 宣纸底 */
--ink: #29251f;      /* 墨 */
--cinnabar: #a33a2b; /* 朱砂 */
--gold: #b18a55;     /* 描金 */
--dai: #3d5960;      /* 黛青 */
--font-kai: "Kaiti SC", "KaiTi", …, serif;   /* 楷体，带回退 */
```

改这几个变量，整站配色与字体一起变。

### 换图片 / 插画

**本项目刻意不含任何图片文件**——月亮、山水、灯笼、桂花、云纹、窗棂全部是 CSS 渐变与内联 SVG 画的，没有外链、没有版权风险。

想换成自己的插画：

- 背景：`client/src/components/Scenery/Background.tsx`（装饰层，`pointer-events: none`）
- 骰子：`client/src/components/Dice/DiceFace.tsx`（SVG）
- 印章：`client/src/components/Common/Seal.tsx`

把对应 SVG 换成你自己的 `<image>` 或 `<path>` 即可。建议把图片放进 `client/public/` 再用 `/xxx.png` 引用。

### 改音效

编辑 **`client/src/audio/AudioManager.ts`**——音效是用 **Web Audio API 现场合成**的（噪声、正弦、衰减包络、滤波器）。所以没有音频文件，也不会有版权问题。

- 觉得某个音不对：改对应的私有方法（`noiseBurst` / `gong` / `woodblock` / `bell` / …）。
- 想换成真实音频文件：在 `play(id)` 里按 `SoundId` 播 `<audio>` 或 `AudioBufferSourceNode` 即可，其他代码不用动。

### 改文案

- 开奖演出的标题/副标题 → `shared/src/awards.ts` 的 `title` / `subtitle` / `rule`
- 无奖的随机文案 → `shared/src/awards.ts` 的 `NONE_MESSAGES`
- 随机雅号池 → `shared/src/schemas.ts` 的 `RANDOM_NICKNAMES`
- 游戏日志文案 → `server/src/game/GameEngine.ts` 的 `writeRollLog()`

---

## 测试

```bash
npm test
```

**232 个测试全部通过。** 覆盖：

| 文件 | 内容 |
| --- | --- |
| `rules.test.ts` | **穷举全部 6⁶ = 46656 种骰子组合**，逐个与一份独立实现的判奖参照比对；并校验每种奖项的组合数等于数学真值（一秀 17400、二举 9300、三红 2500、四进 1875、对堂 720、四点红 360、五红 30、五子登科 150、六抔黑 4、插金花 15…），与公开的博饼概率一致 |
| `dice.test.ts` | 随机数边界（rng 返回 0/1/NaN 不越界）、状元骰型构造不会意外升降级、加权抽取分布、月华概率曲线与封顶、保底 |
| `champion.test.ts` | 追状元队列（长度恒为 N−1、不含状元本人、从下家开始、绕圈）、7×7 反超等级矩阵、月华值视图单调性与文案安全性 |
| `inventory.test.ts` | 库存公式、发奖扣减、领完不补、状元奖延后与幂等 |
| `rooms.test.ts` | 房间码生成/校验/规范化、建房查重与 `MAX_ROOMS`、跨房隔离（玩家 / 阶段 / 广播）、**同一个 playerId 在两间房里互不串线**、断线重连与换房、空房回收 |
| `engine.test.ts` | 端到端边界情况（见下），含散场回收 |

`engine.test.ts` 用 `FakeClock`（可手动推进的时钟）+ 确定性 `Rng`，因此**「30 秒超时」「保底」「掉线代掷」这类平时靠运气的路径都是确定性的**，不会偶发失败。

覆盖的边界情况包括：

- 1 人不能开局 / 2 人能 / 15 人能 / 第 16 人被拒
- 满 15 人 5 秒自动开局；倒计时途中有人离席则取消
- 开局后陌生人无法加入；无效 token 无法加入；有效 token 可重连且座位身份不变
- 等待大厅掉线超 60 秒腾座位；游戏开始后掉线永不腾座位
- 房主离席整桌作废（房间销毁、其余人被请回入席页）
- 非当前回合玩家不能博饼；turnId 不匹配被拒
- **双击按钮只博一次**；同一回合换 actionId 也博不了第二次
- 30 秒无操作自动代掷；**掉线玩家一样自动代掷**；超时后回合持续推进不停摆
- **离线玩家的代掷等待只有 10 秒**；掉线那一刻正好轮到他，截止时间会当场缩短
- 库存充足时扣库存加分记奖品；**库存为 0 时只显示骰型，不发奖不加分**
- **Champion Tier 掷出当时不发奖不加分**（延后到结算）
- 首位状元开追状元；队列长度 N−1 且不含状元；同等级不反超；更高等级反超；**最后一位挑战者仍可反超**
- 只追一圈，走完立即结算
- 状元奖只发一次、正好 +100；被反超者一分不得
- 五个完整轮次内必定出现首位状元（`5N` 次，2 人局是第 10 掷、15 人局是第 75 掷）
- 重开后积分奖品清零、库存重置、座位昵称保留；第二局 turnId 不与第一局撞车
- 快照不泄露 sessionToken、与内部状态解耦、按座位排序
- 房间码打错一个字符 → `ROOM_NOT_FOUND`，且**不会顺手建出一张空桌**
- 换桌后旧桌的人数会扣掉他，不留「在线」幽灵座位

类型检查与构建：

```bash
npm run typecheck    # shared + server + client 全量 tsc --noEmit
npm run build        # 前端产物到 client/dist
```

### 端到端联机验收

单元测试是直接调用 `GameEngine` 的，它证明不了「真的能联机」。所以另有一个验收脚本，起 **4 个真实的 Socket.IO 客户端**连到**真实运行的服务端进程**，跑完整的一局：

```bash
# 终端 A
npm run dev:server
# 终端 B
npm run test:e2e
```

**每次 130~195 项检查全部通过**（牌局长度随骰子而定，逐回合那一节的检查条数会跟着变）。它验证的是单元测试覆盖不到的东西：

- 四个人是否**真的互相看得见**、座位是否按入席顺序排定
- 每一次开奖，**四个人收到的骰子是否完全相同**（逐次比对 `dice.join()` 与 `awardId`）
- `stateVersion` 是否**单调递增**，有没有客户端收到过倒退的状态
- 非当前回合玩家**代博被拒**、同一回合**换 actionId 也博不了第二次**
- 结算：恰好一人是最终状元、只有他持有状元奖品、他的积分 = 其他得分 + 基础分 + 100
- 断线重连：座位 / 积分 / playerId 全部不变，**伪造 token 的 sync 被拒**
- 再来一局：库存重建、计数器归零、**第二局 turnId 不与第一局撞车**

脚本每次**现开一张新桌**，所以不挑服务端状态：不用重启、也不会和别人的牌局互相干扰（如果刚才跑过浏览器验收，直接跑就行）。

指向 Vite 的 5173 端口可以验证开发时手机走的那条代理链路：

```bash
E2E_URL=http://localhost:5173 npm run test:e2e
```

### 多房间隔离验收

单元测试覆盖了 `RoomManager` / `PlayerManager` 的逻辑，但**覆盖不到 socket 路由层**——而「这次开奖到底发给了哪张桌」恰恰是最容易错、又最难在本地发现的地方：一个 `io.to()` 传错参数，两桌人就会开始抢同一副骰子，而单独看任何一张桌都显得挺正常。

```bash
# 终端 A
npm run dev:server
# 终端 B
npm run test:multiroom
```

**66 项检查全部通过**，脚本会开好几张桌同时玩，逐条断言：

- 两张桌拿到**不同的房间码**，且都不含 `0/O/1/I/L`
- 不存在的房间码 → `ROOM_NOT_FOUND`，且房间总数不变（**不会凭空多出一张空桌**）
- 先证明「两桌确实各有一个 `player_0001`」，再证明他们**互不串线**
- 甲桌开席、掷骰，**乙桌收不到任何一条广播**，仍停在 `LOBBY`
- 跨桌动作被挡：拿甲桌的 turnId 去乙桌掷骰、开别人的席、在没开席的桌掷骰，全部被拒
- 中途换桌：**收不到旧桌的广播**了、新桌的照收，且旧桌的人数会扣掉他、不留「在线」幽灵座位
- 站点统计：新面孔入席 `visitors` +1、**同一个人换桌不重复计数**、在线人数不漂移；有人退场 `online` −1 但 `visitors` 不动；人都走光后 `online` 归零而 `visitors` 保留

两个脚本都可以指向 Vite 的 5173 端口，用来验证**开发时手机走的那条代理链路**：

```bash
E2E_URL=http://localhost:5173 npm run test:multiroom
```

### 浏览器验收

协议对了不等于界面画得出来。这一层用**系统已装的 Chrome**（不下载 Playwright 自带的浏览器）真点一遍：

```bash
# 终端 A
npm run build && npm start
# 终端 B
npm run test:browser
```

**92 项检查全部通过，并把每一步截图存到 `screenshots/`。** 它验证的是只有「真的看一眼」才能发现的东西：

- React 有没有挂载、是不是白屏、有没有实际文字
- 控制台**零报错**、零未捕获异常、零加载失败资源
- **零外部网络请求**——这条守住「不 hotlink 任何外部素材」的承诺
- 开桌拿到 5 位房间码，**四个人真的能在同一个大厅里互相看见**（后三个人是走 `?room=` 邀请链接进来的，顺带验证了这条链路），开局后四个人都进了游戏页
- **恰好一个人**的博饼按钮可点（其余人是禁用的）
- 375px 手机视口下**无横向溢出**，且所有按钮点击靶 **≥ 40px**
- **碗里的点数与屏幕上报的奖项自洽**：把屏幕上数到的点数用一份**独立实现的判奖参照**反推奖项，再和屏幕文案比对——骰子画错点数、或者奖项文案对不上点数，静态检查和单元测试都发现不了，只有这一条能发现
- **页脚**：署名是否真的指向 `github.com/gofish040821`、是否显示成 `@gofish040821`、页脚里有没有 `<img>`（0 张才算过）、统计数字有没有出来、以及「此刻在线」有没有把已入席的四个人算进去

> 这几个脚本都要求房间是干净的。房间状态是纯内存的，跑完一轮请重启服务端；脚本启动时会自检，不干净会直接告诉你，而不是让你对着超时发呆。

---

## 常见问题

**Q：为什么同一个浏览器开两个标签页，第二个人进不来？**
身份靠 `localStorage` 里的 `guestId` 区分。同一个浏览器配置文件会被认成同一个人，服务端会提示「这个浏览器已经有座位了」。请用**无痕窗口**或另一个浏览器。

**Q：手机连不上？**
99% 是防火墙。见 [用手机一起玩（局域网）](#用手机一起玩局域网) 的排查步骤。也确认路由器没开 AP 隔离。

**Q：刷新页面会掉座位吗？**
不会。座位凭证存在 `localStorage`，刷新后自动重连回原座位，昵称、积分、奖品、游戏阶段全部恢复。

**Q：某个玩家掉线了怎么办？**
整桌不会被卡住。轮到他时只等 10 秒（在线的人是 30 秒），服务器自动替他掷。他回来只要刷新页面就能接着玩。

**Q：房主退出了会怎样？**
这一桌立刻作废，所有人被请回入席页。注意「退出」包括刷新、切后台和**手机息屏**——服务端只认连接断没断。所以**别用手机开完桌再切去微信发链接**，那一桌会当场散掉；先把朋友拉进来，或者在电脑上开桌。详见 [架构 · 房主离席 = 整桌作废](#房主离席--整桌作废)。

**Q：大家都关了页面，这局会一直卡在那儿吗？**
不会。全员离线满 2 分钟就会判定散场：本局作废，房间退回大厅（详见 [架构 · 掉线与重连](#架构为什么它是真联机)）。这个宽限是刻意的——切后台、Wi-Fi 抖动、手机息屏都会在一两秒内重连回来，那种短暂掉线不该毁掉整局牌。要调这个时间就改 `ABANDON_GRACE_MS`，或起服务时用环境变量覆盖：

```bash
ABANDON_GRACE_MS=60000 npm start
```

**Q：一局大概多久？**
一局通常 5~10 分钟。因为有「月华加持」，首个状元最晚在第 5N 次掷骰时出现，不会无限拖长。

**Q：为什么界面不显示具体的状元概率？**
这是刻意的，而且**全站统一**——不只是月华值那个组件，规则弹窗也只用「月华会随掷骰慢慢蓄满，不必担心久等不来」这样的说法，不写百分比、不写「三个轮次内必定出状元」。原因有两层：一是玩家盯着「还差两把就保底」做最优决策，就不像博饼了；二是**具体概率是随时可调的参数**（见上文改月华加成），写死在文案里，改了参数就会说谎。

想知道确切数值，看 `server/src/config/gameConfig.ts`——那是唯一真相。要验证文案没跑偏，`champion.test.ts` 里有一条测试会断言月华文案不含「必出 / 必中 / 保证 / 一定 / 必定」。

**Q：能同时开多个房间吗？**
能。每一桌就是一个自包含的 `GameEngine` 实例，由 `RoomManager`（`server/src/room/`）按房间码保管，桌与桌之间不共享任何状态——各自有自己的玩家、骰子、库存和计时器。默认最多 200 张（`MAX_ROOMS`）。详细设计见 [架构 · 多房间](#架构为什么它是真联机)。

**Q：房间码会撞车吗？**
字母表刻意去掉了 `0/O/1/I/L` 这些念出来、抄下来都容易错的字符，剩下 31 个，取 5 位——约 2860 万种组合。建房时仍会对着现存房间查重，撞了就重摇，最多试 8 次。房间码由**服务端**生成，客户端不能指定。

**Q：房间开着没人用，会一直占着吗？**
不会。大厅里一个人都没有的桌，闲置满 2 分钟（`EMPTY_ROOM_TTL_MS`）就被回收，连带里面的计时器一起销毁。牌局进行中的桌不会被回收——它有自己的散场判定（见上一条），作废回大厅、人走空之后才轮到回收。

**Q：数据存在哪？**
全部在内存里，没有任何数据库依赖。服务端重启后所有房间清空，客户端会提示重新入席。这是刻意的：一局游戏就几分钟，落库的复杂度不值得。

> 代价要说清楚：免费实例是**单进程**的，所以重启（或在 Render 免费套餐上闲置 15 分钟休眠）会同时清掉所有房间，而不是只影响一桌。对 5~10 分钟一局的牌局来说这个代价可以接受。

**Q：页脚的「人到访」为什么重启后变小了？**
因为它也只在内存里，服务进程重启就归零，重新计起。所以页脚写的是「**自本次开服以来**」，而不是「累计至今」——后者在重启之后就是一句假话。想让它真正跨重启累计，得引入外置存储；本项目的原则是不引数据库，见上面的 [页脚](#页脚站点统计与创作者署名) 一节。

**Q：页脚的数字为什么我这里是 0，别人那里不是？**
每个浏览器只看得见服务端广播给它的那一份，数字本身是全站共享的。刚连上时服务端会先补推一份当前值；如果显示为空（而不是 0），说明那一刻还没收到——留白是刻意的，先画一个 0 出来会被当成真的。

**Q：想改页脚的署名 / 统计口径？**
署名在 `client/src/components/Common/SiteFooter.tsx` 顶部的 `CREATOR` 常量；统计口径在 `server/src/stats/SiteStats.ts`（现在是按 `guestId` 去重的人头数，改成按连接数或改成算 PV 都在那一个文件里）。

---

## 设计取舍

- **为什么用事件 + 全量快照，而不是增量补丁？** 一局最多几百次掷骰，快照体积很小；换来的是「任何客户端在任何时刻拿到快照都能立刻对齐」的强自愈能力。增量补丁省的那点带宽，不值这个复杂度。
- **为什么把月华加持的概率明写在规则里？** 这是一个会影响「公平感」的机制。藏着不说，玩家发现状元来得莫名其妙时会觉得游戏在作弊。写清楚，它就只是一个节奏调节器。
- **为什么是纯 CSS/SVG 美术？** 为了零外部依赖、零版权风险、任意分辨率不糊，以及最重要的一点——`npm install` 之后立刻就能跑，不需要下载任何素材。
- **为什么音效是合成的？** 同上。而且合成音效可以按奖项等级连续调参（`gong` 的音高、衰减时长），比找 13 个音效文件再对齐风格容易得多。
- **为什么把加持概率压到 0.5%~3%？** 状元档的自然概率是 1.2024%（判奖表定死，改不了）。加持要是给到 15%，状元就基本是「机制按时发货」，谁博出来都不算本事了。压到 3% 封顶之后，六成以上的 4 人局状元会落在最后的保底时刻——它重新变成一件值得站起来喊的事。代价是每局必定有状元这件事改不掉（保底决定的），所以能调的是**时机**，不是**有无**。
- **为什么统计不做持久化？** 一局游戏几分钟，为页脚那三个数字引一个数据库，是拿整个项目最干净的那条约束（零外部依赖、`npm install` 就能跑）去换一个装饰性数字。宁可让它在界面上照实写「自本次开服以来」。
- **为什么房主一走就散桌，还一点宽限都不给？** 见 [房主离席 = 整桌作废](#房主离席--整桌作废)。这是一条明确要求换来的取舍：散伙要够干脆，就得接受「刷新一次的房主会把全桌一起带走」。真觉得太狠，加宽限期是一个很小的改动（把 `closeIfHostLeft` 改成起一个定时器即可），但那就得同时回答「宽限期内牌局算谁的」这个问题。

---

## License

MIT
